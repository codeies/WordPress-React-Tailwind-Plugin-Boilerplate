import React from "react";
import App from "./App";
import { render } from '@wordpress/element';
import '../../styles/main.scss';

// Render the App component into the DOM
render(<App />, document.getElementById('eventmaster'));